<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!--  내부 색상  -->
    <solid android:color="@android:color/black"/>

    <!-- 테두리 내부 여백 설정 -->
    <padding
        android:bottom="1dp"
        android:left="5dp"
        android:right="5dp"
        android:top="1dp" />

    <!-- 테두리 설정: 두께, 색상 -->
    <stroke
        android:width="2dp"
        android:color="#D7591A" />

</shape>